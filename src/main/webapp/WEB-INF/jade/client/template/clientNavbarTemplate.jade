p.navbar-text
    | bezpečnostní limit
    span#clientAuthTimeout
li.dropdown
    a#user(href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false")
        span.fa.fa-user
        if displayName
            | #{displayName}
        else
            | Uživatel
        span.caret
    ul.dropdown-menu(aria-labelledby="client")
        li: a(href="../logout")
            span.fa.fa-times.text-danger
            | Odhlásit